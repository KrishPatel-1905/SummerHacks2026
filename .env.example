# Required: MongoDB connection string. The database name may be included here.
MONGODB_URI=mongodb://127.0.0.1:27017/event-capsule

# Optional server settings.
PORT=3000
PUBLIC_APP_URL=http://localhost:3000

# Optional local upload directory. Files are served from /uploads.
# Replace the local storage adapter for production object storage.
UPLOAD_DIR=./uploads
